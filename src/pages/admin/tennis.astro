---
import { supabase } from '../../lib/supabase';
import '../../styles/global.css';
import Layout from '../../layouts/Layout.astro';

const adminPassword = Astro.cookies.get('admin_password');

if (adminPassword?.value !== import.meta.env.ADMIN_PASSWORD) {
  return Astro.redirect('/login');
}

const { data: players } = await supabase
  .from('tennis_players')
  .select('*')
  .order('position', { ascending: true });

let message = '';
let errorMessage = '';

if (Astro.url.searchParams.get('saved') === '1') {
  message = 'Match Tennis registrato con successo!';
}

if (Astro.request.method === 'POST') {
  const body = await Astro.request.text();
  const data = new URLSearchParams(body);

  const winnerName = data.get('winner_name')?.trim() || '';
  const loserName = data.get('loser_name')?.trim() || '';
  const score = data.get('score')?.trim() || '';

  try {
    if (!winnerName || !loserName) {
      throw new Error('Entrambi i giocatori sono obbligatori.');
    }

    if (winnerName === loserName) {
      throw new Error('Un giocatore non può giocare contro se stesso.');
    }

    const { data: allPlayers, error: fetchError } = await supabase
      .from('tennis_players')
      .select('*')
      .order('position', { ascending: true });

    if (fetchError || !allPlayers) {
      throw new Error('Errore nel recupero dei giocatori.');
    }

    const winner = allPlayers.find(
      p => p.name === winnerName
    );

    const loser = allPlayers.find(
      p => p.name === loserName
    );

    if (!winner || !loser) {
      throw new Error('Giocatori non trovati.');
    }

    // CHALLENGER = chi stava sotto
    const challenger =
      winner.position > loser.position
        ? winner
        : loser;

    // DEFENDER = chi stava sopra
    const defender =
      winner.position < loser.position
        ? winner
        : loser;

    const challengerWon =
      winner.id === challenger.id;

    const distance =
      challenger.position - defender.position;

    // Nuove posizioni
    const newPositions = {};

    // Copia stato iniziale
    allPlayers.forEach(p => {
      newPositions[p.id] = p.position;
    });

    // =========================
    // CHALLENGER VINCE
    // =========================

    if (challengerWon) {

      // ===== DISTANZA 1 =====
      if (distance === 1) {

        newPositions[challenger.id] =
          defender.position;

        newPositions[defender.id] =
          challenger.position;
      }

      // ===== DISTANZA 2 =====
      else if (distance === 2) {

        const playerBetween = allPlayers.find(
          p => p.position === challenger.position - 1
        );

        newPositions[challenger.id] =
          challenger.position - 1;

        if (playerBetween) {
          newPositions[playerBetween.id] =
            challenger.position;
        }
      }

      // ===== DISTANZA 3 =====
      else if (distance === 3) {

        const playerBetween = allPlayers.find(
          p => p.position === challenger.position - 1
        );

        newPositions[challenger.id] =
          challenger.position - 1;

        newPositions[defender.id] =
          defender.position + 1;

        if (playerBetween) {
          newPositions[playerBetween.id] =
            challenger.position;
        }

        const playerBelowDefender =
          allPlayers.find(
            p => p.position === defender.position + 1
          );

        if (
          playerBelowDefender &&
          playerBelowDefender.id !== challenger.id
        ) {
          newPositions[playerBelowDefender.id] =
            defender.position;
        }
      }

      // ===== DISTANZA 4 =====
      else if (distance === 4) {

        newPositions[challenger.id] =
          challenger.position - 2;

        newPositions[defender.id] =
          defender.position + 1;

        const p1 = allPlayers.find(
          p => p.position === defender.position + 1
        );

        const p2 = allPlayers.find(
          p => p.position === challenger.position - 2
        );

        const p3 = allPlayers.find(
          p => p.position === challenger.position - 1
        );

        if (p1 && p1.id !== challenger.id) {
          newPositions[p1.id] =
            defender.position;
        }

        if (p2 && p2.id !== challenger.id) {
          newPositions[p2.id] =
            p2.position + 1;
        }

        if (p3 && p3.id !== challenger.id) {
          newPositions[p3.id] =
            p3.position + 1;
        }
      }

      // ===== DISTANZA >= 5 =====
      else {

        newPositions[challenger.id] =
          challenger.position - 2;

        newPositions[defender.id] =
          defender.position + 2;

        const below1 = allPlayers.find(
          p => p.position === defender.position + 1
        );

        const below2 = allPlayers.find(
          p => p.position === defender.position + 2
        );

        if (below1) {
          newPositions[below1.id] =
            below1.position - 1;
        }

        if (below2) {
          newPositions[below2.id] =
            below2.position - 1;
        }

        const jumped1 = allPlayers.find(
          p => p.position === challenger.position - 2
        );

        const jumped2 = allPlayers.find(
          p => p.position === challenger.position - 1
        );

        if (jumped1 && jumped1.id !== challenger.id) {
          newPositions[jumped1.id] =
            jumped1.position + 1;
        }

        if (jumped2 && jumped2.id !== challenger.id) {
          newPositions[jumped2.id] =
            jumped2.position + 1;
        }
      }
    }

    // =========================
    // DEFENDER VINCE
    // =========================

    else {

      const playerBelowChallenger =
        allPlayers.find(
          p => p.position === challenger.position + 1
        );

      newPositions[challenger.id] =
        challenger.position + 1;

      if (playerBelowChallenger) {
        newPositions[playerBelowChallenger.id] =
          challenger.position;
      }
    }

    // =========================
    // UPDATE DATABASE
    // =========================

    for (const p of allPlayers) {

      const newPos = newPositions[p.id];
      const oldPos = p.position;
      
      const positionChange = oldPos - newPos;

      const matchesPlayed =
        p.id === challenger.id ||
        p.id === defender.id
          ? p.matches_played + 1
          : p.matches_played;

      const wins =
        p.id === winner.id
          ? p.wins + 1
          : p.wins;

      const losses =
        p.id === loser.id
          ? p.losses + 1
          : p.losses;

      const { error } = await supabase
        .from('tennis_players')
        .update({
          previous_position: p.position,
          position: newPos,
          matches_played: matchesPlayed,
          wins,
          losses
        })
        .eq('id', p.id);

      if (error) {
        console.log(error);
        throw new Error(error.message);
      }
    }

    // =========================
    // STORICO MATCH
    // =========================

    await supabase
      .from('tennis_matches')
      .insert({
        challenger_id: challenger.id,
        defender_id: defender.id,
        winner_id: winner.id,
        score,
        challenger_old_position:
          challenger.position,
        defender_old_position:
          defender.position,
        challenger_new_position: newPositions[challenger.id],
        defender_new_position: newPositions[defender.id]
      });

    return Astro.redirect(
      '/admin/tennis?saved=1'
    );

  } catch (err: any) {
    errorMessage = err.message;
  }
}
---

<Layout title="Admin Tennis">

  <datalist id="tennis-players-list">
    {players?.map(player => (
      <option value={player.name}>
        {player.name} ({player.position}°)
      </option>
    ))}
  </datalist>

  <section class="mb-8">
    <p class="text-sm uppercase tracking-widest font-black text-slate-600">
      Area riservata
    </p>

    <h1 class="text-5xl md:text-7xl font-black leading-none text-black">
      Admin Tennis
    </h1>

    <p class="mt-4 max-w-xl text-slate-700 font-medium">
      Inserisci il vincitore e lo sconfitto.
      La classifica verrà aggiornata automaticamente.
    </p>
  </section>

  {message && (
    <div
      id="success-banner"
      class="club-card bg-emerald-500 text-black p-4 mb-6 font-black"
    >
      {message}
    </div>
  )}

  {errorMessage && (
    <div class="club-card bg-rose-500 text-black p-4 mb-6 font-black">
      ⚠️ {errorMessage}
    </div>
  )}

  <form
    method="POST"
    action="/admin/tennis"
    class="club-card p-5 md:p-6 max-w-2xl space-y-8 bg-white"
  >

    <div>
      <h2 class="text-2xl font-black mb-3 text-black">
        Vincitore
      </h2>

      <input
        type="text"
        name="winner_name"
        list="tennis-players-list"
        placeholder="Chi ha vinto?"
        required
        autocomplete="off"
        class="w-full border-2 border-black bg-white p-3 font-bold"
      />
    </div>

    <div>
      <h2 class="text-2xl font-black mb-3 text-black">
        Sconfitto
      </h2>

      <input
        type="text"
        name="loser_name"
        list="tennis-players-list"
        placeholder="Chi ha perso?"
        required
        autocomplete="off"
        class="w-full border-2 border-black bg-white p-3 font-bold"
      />
    </div>

    <div>
      <h2 class="text-2xl font-black mb-3 text-black">
        Punteggio
      </h2>

      <input
        type="text"
        name="score"
        placeholder="6-4 7-5"
        class="w-full border-2 border-black bg-white p-3 font-bold"
      />
    </div>

    <button
      type="submit"
      class="club-btn w-full p-4 text-xl"
    >
      Registra Match Tennis
    </button>

  </form>

  <script is:inline>
    const banner =
      document.getElementById(
        'success-banner'
      );

    if (banner) {
      setTimeout(() => {
        banner.remove();

        const url = new URL(window.location);

        url.searchParams.delete('saved');

        window.history.replaceState(
          {},
          '',
          url
        );
      }, 3000);
    }
  </script>

</Layout>